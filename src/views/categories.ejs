<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>
        <h2>Our categories!</h2>
        <ul class="ul-all">
            <% categories.forEach(category => { %>
                <li><%= category.name %></li>
            <% }) %>
        </ul>
    </main>

    <%- include('partials/footer') %>